spring.application.name=Backend
spring.config.import=application-secrets.properties
# The "optional" prexif COULD have been added, but the project lowkey cannot work without the api key
# spring.config.import=optional:application-secrets.properties


stripe.checkout.success-url=http://localhost:3000/?payment=success&session_id={CHECKOUT_SESSION_ID}
stripe.checkout.cancel-url=http://localhost:3000/?payment=cancel
stripe.product.display-name=AI chatbot — one-time access
stripe.product.amount-cents=900
stripe.product.currency=usd
paid.accounts.storage-path=data/paid-accounts.json